<Alloy>
	<Window id="win" backgroundColor="#fff">
		<!-- 
			as noted below, id="index" on this Label will get you a 
			compile time warning indicating that you should't use "index" as
			an ID for the label because it is the view's default ID, which is
			reserved for the first top-level element, in this case the Window
		-->
		<Label id="index">My ID will cause a compile warning</Label>
	</Window>
</Alloy>